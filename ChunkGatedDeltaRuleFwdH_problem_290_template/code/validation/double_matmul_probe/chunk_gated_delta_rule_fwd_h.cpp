// CANN's operator compile driver requires the source basename to match the
// snake_case operator name. Keep the probe implementation separately named
// and include it from this required entry file.
#include "double_matmul_probe.cpp"
